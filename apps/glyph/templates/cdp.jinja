{% extends "base.jinja" %}
{% block title %}CDP records for sign {{ cdp_records[0].sign.sign_ref }}{% endblock %}
{% block content %}
    <div class="container-fluid">
        <h1>Sign: <strong>{{ cdp_records[0].sign.sign_ref }}</strong></h1>
        <div class="row-fluid">
            <div class="span9">
                <h3>This sign occurs in the following {{ cdp_records[0].sign.instances|groupby('tablet.museum_number')|length }} tablets:</h3>
                <ul>
                {% for group in cdp_records[0].sign.instances|groupby('tablet.museum_number') %}
                    <li><strong><a href="{{ url_for(".tablet", tablet_id=group[1][0].tablet.id) }}">{{ group.grouper }} ({{group[1]|length}} instances)</a></strong></li>
                {% endfor %}
                </ul>
            </div>
        </div>
{% endblock %}
{% block sidebar %}
        <h3>CDP Records </h3>
        <table class="table table-striped table-bordered table-condensed">
        <thead>
            <tr><td></td><td>Entries</td></tr>
            <tr><td><strong>Detail</td></tr>
        </thead>
        {%- for row in row_cols %}
            <tr>
            {%- for item in row %}
                <td>{{ item }}</td>
            {%- endfor %}
            </tr>
        {%- endfor %}
        </table>
{% endblock %}
